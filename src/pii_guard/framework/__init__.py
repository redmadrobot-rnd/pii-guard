"""
Generic PII detection framework.

All entity-agnostic infrastructure lives here: base classes, registry,
pattern building, context helpers, conflict resolution, normalisation,
and the Presidio recognizer adapter.

**No** entity-specific code (keywords, validators, patterns) belongs here.
"""
